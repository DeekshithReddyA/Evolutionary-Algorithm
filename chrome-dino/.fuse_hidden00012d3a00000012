/* ══════════════════════════════════════════════
   Chrome Dino AI Lab – dark theme
   ══════════════════════════════════════════════ */

* { margin: 0; padding: 0; box-sizing: border-box; }

body {
  font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
  background: #0d1117;
  color: #e6edf3;
  min-height: 100vh;
}

.screen { min-height: 100vh; }

/* ─── Home Screen ──────────────────────────── */
#home-screen {
  display: flex;
  align-items: center;
  justify-content: center;
}

.home-container { text-align: center; padding: 2rem; }

.home-header h1 {
  font-size: 2.5rem;
  margin-bottom: .5rem;
  background: linear-gradient(135deg, #58a6ff, #d2a8ff);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.home-header p {
  color: #8b949e;
  font-size: 1.1rem;
  margin-bottom: 3rem;
}

.mode-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 1.5rem;
  max-width: 900px;
  margin: 0 auto;
}

.mode-card {
  background: #161b22;
  border: 1px solid #30363d;
  border-radius: 12px;
  padding: 2rem 1.5rem;
  cursor: pointer;
  transition: all .3s ease;
}
.mode-card:hover {
  border-color: #58a6ff;
  transform: translateY(-4px);
  box-shadow: 0 8px 24px rgba(0,0,0,.4);
}
.mode-card.disabled        { opacity: .5; cursor: not-allowed; }
.mode-card.disabled:hover  { border-color: #30363d; transform: none; box-shadow: none; }

.card-icon { font-size: 3rem; margin-bottom: 1rem; }

.mode-card h3 { font-size: 1.1rem; margin-bottom: .5rem; color: #e6edf3; }
.mode-card p  { font-size: .85rem; color: #8b949e; line-height: 1.4; }

/* ─── Game Screen ──────────────────────────── */
#game-screen {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 1rem;
  gap: 1rem;
}

.game-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  width: 1200px;
  padding: .5rem 0;
}
.game-header h2 { font-size: 1.3rem; color: #e6edf3; }

.header-controls { display: flex; align-items: center; gap: .75rem; }

/* ─── Buttons ──────────────────────────────── */
.btn {
  padding: .5rem 1rem;
  border: 1px solid #30363d;
  border-radius: 6px;
  background: #21262d;
  color: #e6edf3;
  cursor: pointer;
  font-size: .9rem;
  transition: all .2s;
}
.btn:hover           { background: #30363d; }
.btn-primary         { background: #238636; border-color: #2ea043; }
.btn-primary:hover   { background: #2ea043; }
.btn-secondary       { background: transparent; border-color: #30363d; }
.btn-toggle          { background: #161b22; border-color: #30363d; }
.btn-toggle.active   { background: #1f6feb; border-color: #388bfd; }
.btn-sm              { padding: .4rem .8rem; font-size: .8rem; }

.toggle-group        { display: flex; gap: 2px; }
.toggle-group .btn-toggle:first-child { border-radius: 6px 0 0 6px; }
.toggle-group .btn-toggle:last-child  { border-radius: 0 6px 6px 0; }

/* ─── Canvas ───────────────────────────────── */
.canvas-wrapper {
  border: 1px solid #30363d;
  border-radius: 8px;
  overflow: hidden;
  background: #fff;
}
#gameCanvas { display: block; }

/* ─── User Score ───────────────────────────── */
.user-score {
  width: 1200px;
  text-align: center;
  font-size: 1.2rem;
  padding: .5rem;
  background: #161b22;
  border: 1px solid #30363d;
  border-radius: 8px;
}

/* ─── Config Panel ─────────────────────────── */
.config-panel {
  width: 1200px;
  background: #161b22;
  border: 1px solid #30363d;
  border-radius: 8px;
  padding: 1rem;
}

.config-row {
  display: flex;
  flex-wrap: wrap;
  gap: 1rem;
  align-items: center;
}

.config-row label {
  display: flex;
  flex-direction: column;
  gap: .25rem;
  font-size: .8rem;
  color: #8b949e;
}

.config-row input,
.config-row select {
  padding: .4rem .6rem;
  border: 1px solid #30363d;
  border-radius: 4px;
  background: #0d1117;
  color: #e6edf3;
  font-size: .85rem;
  width: 130px;
}

/* ─── Stats Panel ──────────────────────────── */
.stats-panel {
  width: 1200px;
  display: flex;
  gap: 1rem;
}

.stats-section {
  flex: 1;
  background: #161b22;
  border: 1px solid #30363d;
  border-radius: 8px;
  padding: 1rem;
}

.stats-section h4 {
  font-size: .9rem;
  color: #58a6ff;
  margin-bottom: .75rem;
  border-bottom: 1px solid #21262d;
  padding-bottom: .5rem;
}

.stats-section div { font-size: .85rem; line-height: 1.8; }

/* ─── Chart ────────────────────────────────── */
.chart-container {
  width: 1200px;
  background: #161b22;
  border: 1px solid #30363d;
  border-radius: 8px;
  padding: 1rem;
}
.chart-container h4 {
  font-size: .9rem;
  color: #58a6ff;
  margin-bottom: .75rem;
}

/* ─── NEAT Viz ─────────────────────────────── */
.neat-viz-container {
  width: 1200px;
  background: #161b22;
  border: 1px solid #30363d;
  border-radius: 8px;
  padding: 1rem;
}
.neat-viz-container h4 {
  font-size: .9rem;
  color: #d2a8ff;
  margin-bottom: .75rem;
}
#neatVizCanvas {
  width: 100%;
  border-radius: 4px;
}

/* ─── Export Controls ──────────────────────── */
.export-controls {
  width: 1200px;
  display: flex;
  gap: .5rem;
  flex-wrap: wrap;
  justify-content: center;
  padding: .5rem;
}

/* ─── Responsive ───────────────────────────── */
@media (max-width: 1250px) {
  .game-header, .config-panel, .stats-panel,
  .chart-container, .neat-viz-container,
  .export-controls, .user-score {
    width: 100%;
    max-width: 1200px;
  }
  .mode-grid { grid-template-columns: repeat(2, 1fr); }
}
